
// Common types for product data
import { ProductCardProps } from "@/components/ProductCard";

// Extended product type with optional fields
export type MockProduct = ProductCardProps;

// Type for category mapping
export type CategoryMapping = Record<string, number[]>;
